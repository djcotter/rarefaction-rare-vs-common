name: snakemake_default
channels:
  - bioconda
  - conda-forge
  - r
  - defaults
dependencies:
  - python=3
  - snakemake
  - bcftools
  - openssl=1.0
  - tabix
  - vcftools
  # R packages:
  - r-base
  - r-tidyverse
  - r-optparse
